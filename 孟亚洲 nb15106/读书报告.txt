                累积分布网络和Derivative-sum-product算法:模型和推理上的累积分布函数图
                                          摘要
我们提出了一个类图形模型的直接代表许多随机变量的联合累积分布函数（CDF），称为累积分布网络（CDN）。这不同于图表的概率密度和质量的功能，两者结合起来的边际概率变量的任何子集是通过计算功能限制在模型中得到的，条件概率对应计算混合衍生物。我们将表明，在CDN条件独立性属性是从导演，无向和因子图的条件独立性性质截然不同，但包括双向图的条件独立性属性。为了在这样的模型进行推断，我们描述了“衍生物积和'（DSP）的消息传递算法中，消息对应于关节CDF的衍生物。然后，我们将适用的CDN学习排名玩家在多人团队为基础的游戏，并提出研究的几个未来发展方向的问题。
关键词：图形模型，消息，passingalgorithm，infer- ENCE
   介绍
概率图形模型提供指定的联合概率密度函数（PDF）定义在许多连续随机变量的图示手段，联合概率质量函数（PMF）许多离散随机变量，或一联合概率分布定义在连续的混合物和离散变量。模型中的每个变量对应于图中的一个节点和边缘之间在图中的节点传送模型中的变量之间的统计依赖关系。图形形式主义允许一个通过检查相应的曲线图中，其中节点的图中的分离意味着相应的变量之间的特定条件独立关系，以获得在一个模型随机变量之间独立性的关系。
表示使用曲线变量子集之间独立性的约束的结果是，联合概率常因素进入功能定义在子集的产物c2011吉姆·C.黄和Brendan J.弗雷。黄和Frey。
   图中的相邻节点。通常情况下，这让我们分解了大量的多元显示tribution成简单功能的产品，这样的推论和预测这些模型的任务也可以简化和高效的算法来执行这些任务能方式实现的。通常情况下，一个复杂的分布在观测变量可以使用带有潜变量的图形模式推出，其中在观测变量联合概率被边缘化了潜在变量获得的构造。用另外的潜变量模型包括具有更紧凑的因式分解形式相比，对于在观测变量的联合概率的优点。然而，这往往是在一个显著较高的计算成本估计和推理的成本，作为附加潜变量通常需要一至任一近似顽固边缘化操作（明卡，2001）或从使用马尔可夫链蒙特卡洛模型采样（MCMC ）方法（尼尔，1993年）。此外，还存在有可能与定义在观测变量任何给定的模型相关联的潜变量模型无限多，问题使得对于任何给定的应用程序的阳离子在模型辨识，计添加潜变量可以经常存在的困难而可以期望，当模型参数将被解释。这些问题可能​​阻碍图形模型的适用性在潜变量存在许多现实世界的问题。
的许多图形模型的另一个可能的限制是，联合PDF / PMF本身可能不适合作为概率模型对于某些应用。例如，在学习级，累积分布函数（CDF）是一个概率表示认为可以自然的类型{X≤X}的不等式事件的概率。关节CDF适合于那些在不等式事件，其中统计依存关系也事件之间存在容易术语描述了这样的问题。这种类型的问题的一个例子是，预测的多人游戏的成果与一队结构（Herbrich，敏卡和Graepel，2007）。与此相反分类或回归的规范问题，在学习上的排名，我们都需要学习一些MAP-平起投入相互依赖的输出变量，让我们不妨来模拟这两个变量的状态和统计之间的依存关系之间的随机排序变量。
鉴于上述情况，在这里我们提出一类图形模型称为累积分布网络工程（CDN），在此我们表示一组观测变量的联合CDF。正如我们将展示，的CDN可以被看作是提供给构造超过观测变量多元分布，而不需要明确地引入潜变量然后边缘化的装置。所述result- ING模型包括一个因式分解形式为联合CDF，在需要回答概率查询和边缘化的主要操作包括分化和计算限制分别，而相比之下，求和/积分以图形模型的PDF与潜变量。此外，该模型作为联合CDF的参数化具有全球标准化的约束可以在本地执行在CDN的各项功能，不像无向图模型的情况下为PDF /保偏光纤的优势。我们将目前的CDN的基本性质和证明，以确定条件独立关系的CDN变量之间的规则是从导演，无向和因子图（明珠，1988年的规则截然不同; Lauritzen的，1996年; Kschischang，弗雷和Loeliger， 2001年）。我们将证明，在一个CDN中条件独立性质包括，但不限于，条件独立属性双向图（Drton和Richardson，2008; Richardson和Spirtes，2002;理查森，2003）。
然后，我们将讨论下的CDN，其中的主要挑战是如何计算的联合民防部队的衍生物进行推理的问题。为此，我们将描述推论的CDN一个消息传递算法，称为基于以前的工作衍生物和积算法（黄和Frey，2008年;黄，2009年）。为了演示的CDN的适用性，我们将使用消息传递算法推理以的CDN应用于学习排名，在这里我们将证明的CDF出现自然为概率模型中，很容易指定要在模型变量之间随机排序约束的问题。
2 累积分布网络
在本节中，我们提出了一个模型与方法学习的多人团队为基础的游戏，如光晕2TM上下文排名。我们的模型代表了两种统计的依赖关系，并在模型中的变量随机排序，如团队的表现和球员个人得分。然后，我们使用的DSP算法来计算每名球员的得分条件两者结合起来。比较到TrueSkillTM和ELO方法因子图模型显示，我们的模型和方法，同时允许进行快速评估和改进测试误差就晕2TM测试版的数据集。
虽然上述方法是提供一种灵活的概率模型和从而允许为易于处理的推理的优点，多元高斯的CDF的选择CDN功能需要
为了利用抽样方法来评估DSP的消息。未来的工作可以集中精力寻找的CDN功能，从而计算衍生品是容易处理的封闭形式的参数化。
3，结论
我们已经提出了CDN作为共同的CDF在许多变量的图形模式。我们已经表明，一CDN的条件独立性特性不同于定向，无向和因子图的独立属性不同。然而，这些特性包括但不限于那些对双向图。然后，我们已经证明，推断在CDN行对应的计算衍生物/有限差。我们所描述的DSP的算法，用于计算这样的衍生物/通过在CDN传递消息，其中每个消息对应于接头的CDF的本地衍生物有限差。
我们使用的CDN提供的图形架构，制订模型和方法的学习中，我们必须考虑到统计厘清的模型变量之间tionships依赖的结构化设置的排名。我们第一次采用了DSP算法的多人游戏，玩家在球队争夺排名的问题。该DSP算法使我们能够计算在给定以前的比赛结果，而占游戏的团队为基础的结构，因此我们能够显示比以前的方法改进的结果玩家的分数分布。 CDN的架构，然后用于构建损失函结构性排名的学习，我们要考虑其出现的排名一组对象的统计相关关系。我们发现，许多概率模型等级数据可以被看作是与成对对象的喜好之间不同连通性特定的CDN。根据提出的工作和结果，我们可以建议研究有关本书稿提出的方法未来的发展方向。
   未来的工作
虽然我们提出用于构造一个联合CDF一个图形模型的框架，可能有一些应用，其中一个可能希望代替优化对数概率密度的logP（X |θ）。我们提出的算法的DSP为离散和连续变量的网络和我们展示了如何的DSP可被用来计算概率密度P（x |＆theta;）从接头CDF F（X |θ）MOD-由CDN ELED。为了进行最大似然的学习中，我们希望以最大化对数似然的L（X;θ）=的logP（X |θ）相对于一个参数矢量θ为一组给定转播的送达变量X，一个也可以使用DSP的消息的修改版本以计算梯度∇θL（X;θ）对数似然的。的指导原则这里是梯度算子可以是分布式之间在CDF本地函数，很像在DSP中的微分运算，使得通过修改DSP消息适当地，我们可以得到梯度∇θL（X;θ）。完成计算后，梯度矢量可以被用来在一个梯度下降算法来优化对数似然。未来的研究在这个方向可能是建立什么类图中可以产生听话梯度计算，以及参与comput- ING梯度图与周期的复杂性/精度权衡定向。
我们已经表明，我们的消息传递算法导致正确的一组联合的CDF的衍生物，条件是底层的图是一个树。由于与和积算法的因子图中，如果图包含周期，则衍生物积和不再保证得到关节CDF的正确混合的衍生物，以便消息可能开始“振荡”，因为它们传播围绕图中的周期。追求的一个重要方向是建立条件下循环的存在将不会导致在消息振荡：人们可以诉诸一个类似的方法作为由Weiss和弗里曼（2001），在那里与循环的曲线图是“展开”，将所得的消息进行分析采用。
我们发现，对于定义在连续变量曲线图中，计算DSP消息更新在给定的功能节点的复杂性成倍增加与相邻变量节点的数目，因为一个人来总结以上消息从变量连接到所有的子集传入产品功能节点。然而，它可以是可能近似用SIM- PLER，易于处理的形式的消息，例如有条件的单变量高斯的CDF。未来的工作在这里将是建立听话的方法来执行这样的近似和评估的推断这种近似计划在合成和真实世界的数据的CDN的性能。
正如我们已经证明，图表分离标准评估的CDN条件独立性包括那些双向图（Richardson和Spirtes，2002）。作为这种图形是含有无向，定向和双向边混合图的特殊情况下，研究的一个未来的途径将是调查是否一个也可以使用混合的图形制剂在CDN模型与相结合tractably近似这种混合图形模型因子图的联合概率密度/质谱功能。通过席尔瓦和Ghahramani（2009年b）贝叶斯学习方法，可以提供与定性和定量比较构造这种混合的图形模型使用的CDN的框架。
